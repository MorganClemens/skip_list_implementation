#include <iostream>
#include "../code/SkipList.h"

using namespace std;

int main() {
    // In the future, you could create and use a SkipList object here
    // SkipList<int> list;
    // list.insert(42);

    return 0;
}